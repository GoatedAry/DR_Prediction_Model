# Core
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.4
joblib>=1.3

# Gradient boosting (preferred backends; sklearn fallbacks are used
# automatically in modeling/models.py if these aren't installed)
xgboost>=2.0
lightgbm>=4.0

# Explainability (SHAP preferred; permutation-importance fallback otherwise)
shap>=0.44

# LLM layer (Groq -- fast, free-tier-friendly, OpenAI-compatible API)
groq>=0.11
pydantic>=2.0

# API wrapper
fastapi>=0.110
uvicorn>=0.29

# Testing
pytest>=8.0
